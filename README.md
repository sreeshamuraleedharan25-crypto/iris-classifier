# Iris Classifier (Decision Tree)

## Overview
End-to-end machine learning example using the Iris dataset and a Decision Tree classifier.

## Quick Start
```bash
git clone https://github.com/<YOUR_USERNAME>/iris-classifier.git
cd iris-classifier
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python src/train.py
